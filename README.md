# VisMat
Mathematics app that allows the user to enter variables (include sliders) and the output will be numerical and visual. The visual references or displays can be user selectable and cover a few domains of STEM.
